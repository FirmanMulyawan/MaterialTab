import React from 'react'
import {StyleSheet, Text, View} from 'react-native'

const AreaLima = () => {
  return (
    <View style={styles.container}>
      <Text>AreaLima</Text>
    </View>
  )
}

export default AreaLima

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
})
